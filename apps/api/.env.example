# Leave MONGODB_URI unset for local dev — the API will spin up an
# in-memory MongoDB automatically (via mongodb-memory-server).
# When you're ready to go live, set this to your real Atlas connection string.
MONGODB_URI=

PORT=4000

# Comma-separated list of origins allowed to call this API
CORS_ORIGIN=http://localhost:3000
